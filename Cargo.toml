[package]
name = "zero2prod"
version = "0.1.0"
edition = "2024"

[lib]
path = "src/lib.rs"

[dependencies]
actix-web = "4.13.0"
tokio = { version = "1.51.1", features = ["full"] }

[dev-dependencies]
reqwest = "0.12" # Or whatever the current stable release is
